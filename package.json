{
  "name": "psd-to-figma-importer",
  "version": "1.0.0",
  "description": "Import PSD files into Figma with editable text and pixel-perfect accuracy",
  "main": "dist/code.js",
  "scripts": {
    "build": "npm run build:code && npm run build:ui",
    "build:code": "esbuild src/code.ts --bundle --outfile=dist/code.js --format=iife",
    "build:ui": "copy src\\ui.html dist\\ui.html",
    "watch": "npm run build && nodemon --watch src --exec npm run build"
  },
  "keywords": ["figma", "plugin", "psd", "photoshop", "import"],
  "author": "PSD to Figma Team",
  "license": "MIT",
  "devDependencies": {
    "@figma/plugin-typings": "^1.94.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}